{% extends 'base.html.twig' %}

{% block title %}Cart!
{% endblock %}

{% block body %}
	<div class="container">

		<div class="card">
			<div class="row">
				<div class="col-md-8 cart">
					<div class="title">
						<div class="row">
							<div class="col">
								<h1>
									<b>Your Cart :
									</b>
								</h1>
							</div>
							<div
								class="col align-self-center text-right text-muted">
								{# {{ products|length }} #}
								product(s)</div>
						</div>
					</div>
					{# {% for successMessage in app.flashes('success') %} #}
					<div class="row">
						<div
							class="col-12">{# <div
																																																																							class="alert alert-success">
																																																																							{{ successMessage|raw }}
																																																																						</div> #}
						</div>
					</div>
					{# {% endfor %}  #}
					<div
						class="row border-top border-bottom">
						{# {% for product in products %} #}
						<div class="row main align-items-center">
							<div
								class="col-2">
								{# {% if product.getImages()|length > 0 %}
																																																																																																																														                {% for image in product.getImages() %}
																																																																																																																														                    <img src="/uploads/products/{{product.getId}}/{{image.getName}}" alt="{{ product.name }}" width="100">
																																																																																																																														                {% endfor %}
																																																																																																																														                {% else %} #}
								<p>Aucune image disponible</p>
								{# {% endif %}</div> #}
								<div class="col">
									<div class="row text-muted">Name
										{# {{ product.name }} #}
									</div>
									<div class="row">
										Description
									{# {{ product.description|slice(0, 10) }} #}
										{# {% if product.description|length > 100 %}
																																																																																																																																																																		            {% endif %} #}
									</div>
								</div>
								<div class="col">
									<a href="#">-</a>
									<a href="#" class="border">1</a>
									<a href="#">+</a>
								</div>
								<div class="col">Price
									{# &euro;{{ product.prix }} #}
									<a href={# "{{ path('panier_supprimer_produit', {productId: product.id}) }}" #} class="btn btn-sm btn-light"><img src={{asset('media/marketing/delete.png')}} alt="delete_panier" style="vertical-align: middle;" width="30" height="30"></a>
								</div>
							</div>
							{# {% endfor %} #}
						</div>
					</div>
					<div class="col-md-4 summary">
						<div>
							<h5>
								<b>Summary</b>
							</h5>
						</div>
						<hr>
						<div class="row">
							<div
								class="col">
								{# {{ products|length }} #}
								product</div>
							<div class="col text-right">&euro;
																																											Total
								{# {{ panier.getTotal() }} #}
							</div>
						</div>
						<form>
							<p>Shipping</p>
							<select>
								<option class="text-muted">Standard-Delivery- &euro;5.00</option>
								<option class="text-muted">Collissimo- &euro;7.00</option>
							</select>
							<p>Code Promo</p>
							<input id="code" placeholder="Your code">
						</form>
						<div class="row" style="border-top: 1px solid rgba(0,0,0,.1); padding: 2vh 0;">
							<div class="col">TOTAL</div>
							<div class="col text-right">&euro;
								{# {{ panier.getTotal() }} #}
							</div>
						</div>
						{# <a href="{{ path('payment', {amount: panier.getTotal()}) }}"> #}
						<button class="btn btn-success mt-4">Paiement</button>
					</a>
				</div>
			</div>
		</div>

	{% endblock %}
